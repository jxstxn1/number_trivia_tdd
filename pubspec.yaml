name: number_trivia_tdd
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  dartz: ^0.10.0
  data_connection_checker:
    git: 
      url: git://github.com/bobanalappat/data_connection_checker.git
      ref: master
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^7.2.0
  get_it: ^7.2.0
  http: ^0.13.3
  mockito_builder_annotations: ^0.2.2
  shared_preferences: ^2.0.7

dev_dependencies:
  build_runner: ^2.1.1
  flutter_test:
    sdk: flutter
  lint: ^1.6.0
  mockito: ^5.0.15

flutter:
  uses-material-design: true
